# Core dependencies (always required)
pydantic>=2.0
rapidfuzz>=3.0
networkx>=3.0

# Install optional groups via: pip install -e ".[miner]", ".[brain]", ".[dashboard]", ".[dev]"
# Or install everything: pip install -e ".[all]"
